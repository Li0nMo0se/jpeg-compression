# JPEG algorithm implementation in Python

Take a look at ```Main.ipynb```. The algorithm is explained there.

If you want to run the notebook yourself, run ```pip install -r requirements.txt``` first.
